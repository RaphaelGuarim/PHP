{% extends 'base.html.twig' %}

{% block title %}Index Cart{% endblock %}

{% block body %}
    <h1>Index Cart</h1>

    <h2>NB Cart - {{ carts|length }}</h2>

    <div>
        {% for cart in carts %}
                <a href="/cart/{{ cart.id }}">
                <img src="https://cdn.icon-icons.com/icons2/1678/PNG/512/wondicon-ui-free-parcel_111208.png" alt="Item" ></a>
                <h2>Command N°{{ cart.id }}</h2>
                {#<a href="/cart/{{ cart.id }}/edit">Edit</a>#}
                {#<a href="/cart/{{ cart.id }}/delete">Delete</a>#}
            
        {% else %}
            <b>There are no carts</b>
        {% endfor %}
    </div>
{% endblock %}